@model OnlineResumeBuilder.Web.Models.Skill

@{
    ViewData["Title"] = "Add Skill";
}

<div class="section-container">
    <h2 class="section-title">Add Skill</h2>
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="mb-3">
                            <label asp-for="SkillName" class="form-label"></label>
                            <input asp-for="SkillName" class="form-control" />
                            <span asp-validation-for="SkillName" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="SkillType" class="form-label"></label>
                            <select asp-for="SkillType" asp-items="ViewBag.SkillTypes" class="form-select"></select>
                            <span asp-validation-for="SkillType" class="text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="UserInfoId" class="form-label">User</label>
                            <select asp-for="UserInfoId" class="form-select" asp-items="ViewBag.UserInfoId"></select>
                        </div>
                        <div class="d-flex justify-content-between">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Save
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Back to List
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Tips</h5>
                </div>
                <div class="card-body">
                    <p>Add skills that are relevant to your career goals and highlight your technical expertise.</p>
                    <p>Choose the most appropriate skill type from the dropdown to categorize your skill correctly.</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 